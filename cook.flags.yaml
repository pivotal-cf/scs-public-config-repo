flags:
  ui-theme:
    state: ENABLED
    variants:
      dark: "dark-mode"
      light: "light-mode"
      high-contrast: "hc-mode"
    defaultVariant: high-contrast

  premium-feature:
    state: ENABLED
    variants:
      "enabled": true
      "disabled": false
    defaultVariant: "disabled"
    targeting:
      if:
        - ends_with:
            - var: email
            - "@cook.com"
        - "enabled"
        - "disabled"

  api-rate-limit:
    state: ENABLED
    variants:
      "low": 100
      "standard": 500
      "high": 1000
      "unlimited": 999999
    defaultVariant: "standard"
